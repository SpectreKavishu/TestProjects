# Java AI Architect Rule
- Always use **Java 21** features (Virtual Threads for performance).
- Use **LangChain4j** for all AI integrations.
- All SQL queries must be optimized for **PostgreSQL with pgvector**.
- Never write code without first creating a **JUnit 5** test stub.
